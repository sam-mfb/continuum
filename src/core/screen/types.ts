export type ScreenState = {
  /** X position of the viewport's top-left corner in world coordinates */
  screenx: number
  /** Y position of the viewport's top-left corner in world coordinates */
  screeny: number
}
